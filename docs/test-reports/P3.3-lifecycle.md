# P3.3 Lifecycle Test Report — NPC market corridor and deterministic shocks

## Result

`PASS`

## Test identity

| Field | Value |
|---|---|
| Mode | `task-smoke` |
| Phase/task | Phase 3 / `P3.3` — fixed-point, mean-reverting NPC market index and two server-owned deterministic shocks |
| Commit/build | `2c9dcc6` (`codex/p3-3-market-shocks`) plus uncommitted P3.3 work |
| Formula/content version | `balance-1.2` / `asteria-baseline-0.2` |
| Environment | Windows 11; Node `v24.16.0`; pnpm `11.19.0`; local Docker PostgreSQL/Redis healthy on configured loopback ports |
| Seed/fixture | D-029; `packages/simulation/fixtures/market-v1.json`; server-owned E01 at completed cycle `120` (`+900`) and E02 at `300` (`-800`) |
| Date/agent | `2026-08-16T21:46:05Z`; `lifecycle_game_tester` |

No Phase 3 gate report exists yet because the phase remains `ACTIVE`; this is a task smoke, not a phase-regression or release-acceptance run.

## Player-facing journeys and invariants checked

| Journey/invariant | Expected observable behavior | Result | Evidence |
|---|---|---|---|
| Fresh market state | A new save/migration produces one world-bound market index at `1.0000`, matching the world content/formula versions and revision `1`. | PASS | Disposable PostgreSQL migration runner validated the exact initial row. |
| Corridor endpoints and invalid state | Forced low/high shocks cannot push the stored or calculated index beyond `0.8500`/`1.1500`; an out-of-corridor index, version mismatch, or market state leading the completed world cycles is rejected. | PASS | Simulation property sweep and disposable migration constraint probes passed. |
| Mean reversion and deterministic shocks | The fixed-point fixture agrees with the independent Python reference. E01 and E02 occur only at their declared completed cycles; no random/player-provided shock is accepted. | PASS | TypeScript market suite and both fixture-reference invocations passed. |
| Returning world / ordered catch-up | A pre-existing world at completed cycle `119` can receive the P3.3 migration, then reaches cycle `120` with the ordered E01 result (`1.0900`); one-shot catch-up equals sequential advancement. | PASS | Disposable worker integration initialized a pre-market returning world, reapplied the migration, and asserted the resulting persisted state. |
| Interruption and retry recovery | A transaction that advances the world/market and rolls back leaves both records unchanged; retry advances the pair atomically. | PASS | Worker integration performed a rolled-back cycle-120 transaction, observed the original world/market values, then retried successfully. |
| Concurrent worker and restart | Two independent worker pools at the same server time yield exactly one advancing call; a new worker process/retry and a backwards server time do not advance the market twice. | PASS | Worker integration and locking-adapter unit test passed. |
| Upgrade/rollback recovery | The latest market migration rolls back only while pristine; singleton deletion and rollback after an advanced market state are rejected. Previous NPC/world rollback guards remain intact. | PASS | Disposable migration runner passed forward, repeated rollback/reapply, immutable-state, and advanced-state refusal paths. |
| Smallest critical regression | The existing world-cycle deterministic contract and TypeScript API/worker package checks remain valid. | PASS | Simulation, worker unit, API typecheck, and worker typecheck commands passed. |

## Commands

| Command | Exit code | Summary |
|---|---:|---|
| `pnpm run services:check` | 0 | Local PostgreSQL and Redis health checks passed. |
| `pnpm --filter @nexus/simulation run test` | 0 | Existing PCG/energy/accrual/world checks and the P3.3 market property suite passed. |
| `node scripts/market-fixture-check.mjs` | 0 | TypeScript fixed-point fixture matched D-029. |
| `python tools/verify_market_fixture.py` | 0 | Independent Python fixed-point reference matched the same fixture. |
| `pnpm --filter @nexus/api exec tsx --env-file-if-exists=../../.env scripts/migrations-integration.ts` | 0 | Fresh migration, invalid input, migration upgrade/reapply, rollback and persistence guards passed in a disposable database. |
| `pnpm --filter @nexus/worker run test` | 0 | Forward-only, locked world/market adapter contract passed. |
| `pnpm --filter @nexus/worker run test:integration` | 0 | Disposable PostgreSQL rollback, restart, concurrent-worker, catch-up and backward-clock lifecycle paths passed. |
| `pnpm --filter @nexus/simulation run typecheck` | 0 | Simulation package type check passed. |
| `pnpm --filter @nexus/api run typecheck` | 0 | API package type check passed. |
| `pnpm --filter @nexus/worker run typecheck` | 0 | Worker package type check passed. |
| `pnpm run plan:check` | 0 | Plan validation passed: 11 phases, 83 tasks, 27 completed tasks, 29 decisions. |

The runner reported the absent optional `../../.env` file and continued with the repository's local synthetic connection default; no secret value was required or emitted.

## Defects and regressions

No P0 or P1 defect was observed.

## Explicitly not verified

- P3.3 deliberately has no player-facing market screen, contract board, authenticated market endpoint, browser interaction, or accessibility change. Those player paths are **NOT VERIFIED** and belong to later planned tasks/phases.
- `node scripts/http-smoke.mjs`, the complete root `pnpm verify`, browser E2E, performance, backup/restore, telemetry, and release compatibility were not independently completed by this task-smoke runner. A concurrent full verification owned those ephemeral HTTP ports; these are **NOT VERIFIED** here, not failures of P3.3 behavior.
- P3.4–P3.8 remain **NOT VERIFIED**: contract types/offers, collateral/rewards, archive behavior, event log, NPC adaptation, and 120-day phase stress.

## Gate result

`PASS`
