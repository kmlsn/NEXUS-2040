# P2.6 Lifecycle Test Report

## Result

`PASS`

## Test identity

| Field | Value |
|---|---|
| Mode | `task-smoke` |
| Phase/task | Phase 2 / `P2.6` — center screen resource reasons, production estimates, and energy-shortage explanation |
| Date | 2026-08-14 |
| Runtime/build identifiers | Windows host; Vite `8.2.1`; production bundle `dist/assets/index-BPWwCO1N.js` (`192.53 kB`, gzip `60.73 kB`) |
| Formula/content version | `balance-1.2` / `asteria-baseline-0.2` |
| Seed | Not applicable: this UI/API smoke has no RNG input. |

## Player-facing journeys and invariants checked

| Journey/invariant | Result | Evidence |
|---|---|---|
| Center screen player path | PASS | Web test reported accessible status, exact production estimates, resource reasons, and recovery states. This covers the changed visible P2.6 path, including its invalid/unavailable-data recovery presentation. |
| Fresh/unconfigured local API context | PASS | The loopback API smoke passed with public-error handling; the P2.6 contract is the safe unconfigured `GET /v1/center` 404 with no client-supplied profile identity. |
| Production web startup and shell availability | PASS | Production Vite build and loopback web smoke both exited `0`. |
| API/worker interruption boundary | PASS | Loopback HTTP smoke exited `0`, covering API/worker health, correlation, public errors, and structured logs. |
| Temporary-server recovery | PASS | After both smoke scripts, there were no listening sockets on ports `3100` or `3101`. |

## Raw commands and results

| Command | Exit code | Raw outcome |
|---|---:|---|
| `pnpm --filter @nexus/web run test` | 0 | `PASS: center screen exposes accessible status, exact estimates, reasons, and recovery states.` |
| `pnpm --filter @nexus/web run build` | 0 | Vite `8.2.1` built 16 modules successfully in 196 ms. |
| `node scripts/web-smoke.mjs` | 0 | `PASS: web production shell liveness verified.` |
| `node scripts/http-smoke.mjs` | 0 | `PASS: API/worker health, request correlation, public errors, and structured logs verified.` |
| Post-run listener probe for `3100`, `3101` | 0 | `PASS: no listeners remain on ports 3100 or 3101.` |

## Defects

No P0 or P1 defect was observed in this smoke scope.

## Explicitly not verified

- Returning-profile persistence, live database-backed center values, and retry after a persisted request interruption: not exercised by the requested loopback-only command set.
- Boundary-value and deterministic-seed economy calculations: unchanged P2.6 display path and outside this narrow smoke; covered by prior economy scope or pending P2.7/P2.8 as applicable.
- Manual browser keyboard traversal, focus order, color-independent state, reduced motion, browser matrix, performance benchmark, full E2E, migration, backup/restore, and release acceptance: not part of task-smoke; remain **NOT VERIFIED**.

