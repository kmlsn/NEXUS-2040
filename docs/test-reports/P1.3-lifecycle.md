# P1.3 Yaşam Döngüsü Test Raporu

## Kimlik

| Alan | Değer |
|---|---|
| Mod | `task-smoke` |
| Faz/görev/sürüm | Faz 1 / `P1.3` / yerel PostgreSQL–Redis geliştirme ortamı |
| Commit/build | Commit yok; çalışma ağacı izlenmeyen proje dosyaları içeriyor |
| Formül/içerik sürümü | Uygulanamaz; P1.3 oyun formülü veya içerik eklemez |
| Ortam | Windows, Docker Compose; PostgreSQL `17.7-alpine`, Redis `7.4.3-alpine`; localhost portları `15432` ve `16379` |
| Seed/fixture | Geçici `lifecycle_p13_smoke` PostgreSQL satırı ve `lifecycle:p1.3:smoke` Redis anahtarı; denetim sonunda temizlendi |
| Tarih ve ajan | 2026-08-10 UTC, `lifecycle_game_tester` |

## Kapsam ve yolculuklar

| Yolculuk/invariant | Beklenen | Sonuç | Kanıt |
|---|---|---|---|
| Yerel hizmetleri tekrar başlatma | `pnpm services:up` çalışan servislerde de güvenle tamamlanmalı ve her iki healthcheck yeşile dönmeli | `PASS` | `pnpm run services:up` exit `0`; Compose iki konteyner için `Healthy` bildirdi. |
| PostgreSQL erişilebilirliği | Varsayılan sentetik yerel kullanıcı/veritabanı kabul edilmeli ve SQL sorgusu çalışmalı | `PASS` | `pg_isready -U nexus_local -d nexus_local` accepting connections; `SELECT current_database(), current_user, 1 + 1` = `nexus_local|nexus_local|2`. |
| Redis erişilebilirliği | Yerel Redis healthcheck `PONG` dönmeli | `PASS` | `redis-cli ping` = `PONG`; `pnpm run services:check` exit `0`. |
| Servis kesintisi sonrası kalıcılık | PostgreSQL volume ve Redis AOF verisi, iki konteyner yeniden başlatılıp sağlık beklenince okunabilmeli | `PASS` | Geçici PostgreSQL satırı ve Redis anahtarı yazıldı; `docker compose restart postgres redis` ve `up -d --wait` sonrasında ikisi de `retained` olarak okundu. Smoke verisi ardından silindi. |
| Son durum | Smoke sonunda her konteyner `healthy` ve yalnız `127.0.0.1` port bağında kalmalı | `PASS` | `docker compose ... ps`: PostgreSQL `127.0.0.1:15432->5432`, Redis `127.0.0.1:16379->6379`; ikisi de `healthy`. |

## Komutlar

| Komut | Exit code | Özet |
|---|---:|---|
| `pnpm run test` | 0 | Workspace sözleşmesi ve yerel servis yapılandırması doğrulandı. |
| `pnpm run test:integration` | 0 | Çalışan PostgreSQL ve Redis healthcheck'leri geçti. |
| `pnpm run services:up` | 0 | Çalışan ortam tekrar çalıştırıldı; iki servis healthy durumda kaldı. |
| `docker compose -f infra/docker-compose.yml exec -T postgres pg_isready -U nexus_local -d nexus_local` | 0 | PostgreSQL bağlantı kabul ediyor. |
| `docker compose -f infra/docker-compose.yml exec -T postgres psql ... SELECT current_database(), current_user, 1 + 1` | 0 | Sentetik yerel kullanıcı ve veritabanıyla SQL sorgusu geçti. |
| `docker compose -f infra/docker-compose.yml exec -T redis redis-cli ping` | 0 | Redis `PONG` döndü. |
| `docker compose -f infra/docker-compose.yml restart postgres redis` ardından `up -d --wait` | 0 | İki servis yeniden başladı ve healthcheck'leri geçti. |
| PostgreSQL/Redis geçici veri yazma, yeniden başlatma, okuma ve temizleme | 0 | Her iki kalıcı volume yolu doğrulandı; test verisi kaldırıldı. |
| `pnpm run services:check` | 0 | Son sağlık denetimi geçti. |

## Bulgular ve regresyonlar

| Kimlik | Şiddet | Yeniden üretim | Beklenen/gerçek | Durum |
|---|---|---|---|---|
| Yok | — | — | P0/P1 bulgusu gözlenmedi. | Kapalı |

## Atlanan veya kullanılamayan kontroller

- API ve worker uygulama sağlık uçları `NOT VERIFIED`: bunlar P1.6 kapsamındadır; P1.3 yalnız yerel bağımlı servis ortamını kurar.
- Migration, geri alma, ledger idempotency ve eşzamanlılık `NOT VERIFIED`: P1.4 kapsamındadır; test sırasında yalnız geçici service-level kalıcılık verisi kullanıldı ve silindi.
- Oyuncu profili, oyun yolculuğu, deterministik seed ve tarayıcı E2E `NOT VERIFIED`: P1.3 bunları eklemez.

## Kapı sonucu

`PASS`
