# P2.1 Yaşam Döngüsü Test Raporu

## Kimlik

| Alan | Değer |
|---|---|
| Mod | `task-smoke` |
| Faz/görev/sürüm | Faz 2 / `P2.1` / beş kaynak PostgreSQL ledger; geçerli migration zinciri `003_resource_balances → 004_ledger_integrity → 005_idempotency_completion_integrity` |
| Commit/build | `6a910d6`; test anında P2.1 çalışma ağacı değişiklikleri kaydedilmemişti |
| Formül/içerik sürümü | `balance-1.2` / `asteria-baseline-0.2` |
| Ortam | Windows; Node.js `v24.16.0`; pnpm `11.19.0`; Docker Compose PostgreSQL/Redis; Intel i7-7820HQ sınıfı CPU, 31.9 GiB RAM |
| Seed/fixture | Ledger yolu RNG kullanmaz. İzole disposable PostgreSQL veritabanları: `nexus_lifecycle_p21_trace`, `nexus_lifecycle_p21_journey`, `nexus_ledger_test`, `nexus_migration_test`; rastgele profil/işlem UUID’leri |
| Tarih ve ajan | 2026-08-14T08:51:04Z, `lifecycle_game_tester` |

## Kapsam ve yolculuklar

| Yolculuk/invariant | Beklenen | Sonuç | Kanıt |
|---|---|---|---|
| Geri dönen profil/migration kurtarma | 002’deki geçmiş ledger, 003→004→005 ile beş bakiye satırına doğru backfill edilir; tamamlanmış istek doğru işlemle bağlanır. | PASS | Bağımsız trace: Enerji `200`, Sermaye `600`, diğer üç kaynak `0`; `completed_transaction_id` eşleşen geçmiş transaction UUID’si oldu. |
| Negatif geçmiş kurtarma sınırı | Negatif geçmiş toplamı migration ile kabul edilmez. | PASS | İzole `nexus_lifecycle_p21_negative` probe, 004 uygulamasını reddetti. |
| Yeni profil: beş kaynak hibesi | Tek atomik hibe beş kaynakta `1_000_000` mikro-birim bakiye oluşturur. | PASS | Bağımsız `nexus_lifecycle_p21_journey` probe. |
| Geri dönen profil: çok-kaynaklı harcama | Harcama yalnız istenen bakiyeleri doğru günceller. | PASS | Enerji `999800`, Sermaye `999500`; diğer kaynaklar değişmedi. |
| Kesinti/yeniden deneme | Aynı scope/anahtar/niyet ilk transaction’ı döner, ikinci değer üretmez. | PASS | Bağımsız probe aynı transaction UUID’si ve `replayed=true` gözlemledi. |
| Geçersiz girdi ve değişmiş tekrar | Kesirli mikro-birim/scope ve aynı anahtarla değişmiş delta reddedilir. | PASS | Bağımsız probe reddetmeleri gözlemledi. |
| Sınır/atomiklik | Yetersiz çok-kaynaklı harcama reddedilir; hiçbir bakiye değişmez. | PASS | Başarısız isteğin öncesi/sonrası beş `resource_balances` satırı birebir eşleşti. |
| Tamamlanmış istek bütünlüğü | Tamamlanmış isteğin transaction bağını null yapma girişimi reddedilir. | PASS | Bağımsız probe, 005 completion-state kısıtının reddini gözlemledi. |
| Kritik regression: yarış, immutable ledger, ileri/geri migration | Eşzamanlı harcamada tek commit, ledger değişmezliği ve migration geri dönüş koruması devam eder. | PASS | `pnpm --filter @nexus/api run test:integration` ve `pnpm verify` exit `0`. |
| Kalıcılık/kurtarma temizliği | Disposable test veritabanları sona erince kalmaz; servis penceresi sağlıklıdır. | PASS | Katalog sorgusu `temporary-lifecycle-dbs-absent`; son `pnpm services:check` exit `0`. |

## Komutlar

| Komut | Exit code | Özet |
|---|---:|---|
| `py -3 .agents/skills/verify-game-phase/scripts/validate_plan.py GAME_PLAN.md` | 0 | 11 faz, 83 görev, 17 tamamlanmış görev ve 17 karar tutarlı. |
| `pnpm services:up` | 0 | PostgreSQL ve Redis healthy servis penceresi açıldı. |
| `pnpm services:check` | 0 | Son stabil pencerede PostgreSQL/Redis healthy. |
| `pnpm --filter @nexus/api run test` | 0 | Mikro-birim dönüşümü ve PostgreSQL `bigint` sınırları geçti. |
| `pnpm --filter @nexus/api run test:integration` | 0 | 003→004→005 migration, idempotency, immutable ledger, rollback ve eşzamanlı harcama regresyonları geçti. |
| `$env:TRACE_PROBE=<disposable inline TypeScript>; pnpm --filter @nexus/api exec tsx --eval 'eval(process.env.TRACE_PROBE)'` | 0 | Geçmiş profil backfill ve 005 completion bağını doğrudan izledi. |
| `$env:JOURNEY_PROBE=<disposable inline TypeScript>; pnpm --filter @nexus/api exec tsx --eval 'eval(process.env.JOURNEY_PROBE)'` | 0 | Yeni/geri dönen profil, retry, geçersiz girdi, atomiklik ve completion-integrity yolculukları geçti. |
| `pnpm verify` | 0 | Lint, typecheck, unit, API/worker/web smoke, integration, fixture ve plan denetimleri geçti. |
| `docker compose -f infra/docker-compose.yml exec -T postgres psql ... pg_database ...` | 0 | `temporary-lifecycle-dbs-absent`. |
| İlk iki `$env:LIFECYCLE_PROBE=<disposable inline TypeScript>; ... tsx --eval ...` denemesi | 1 | Sırasıyla ESM-eval kurulum hatası ve servis penceresi kesintisi; uygulama davranışı test edilmedi. |
| Sonraki üç aynı disposable probe denemesi | 1 | Sırasıyla artık geçersiz 004-son migration listesi varsayımı, pool cleanup olayı ve enum-sırası assertion’ı; trace/journey probe’ları ile düzeltildi. Bunlar ürün kusuru değildir. |

## Bulgular ve regresyonlar

| Kimlik | Şiddet | Yeniden üretim | Beklenen/gerçek | Durum |
|---|---|---|---|---|
| Yok | — | — | P0/P1 (veya başka) ürün kusuru gözlenmedi. | Kapalı |

## Atlanan veya kullanılamayan kontroller

- Tarayıcı/API üzerinden görünür kaynak ekranı ve uçtan uca profil yolculuğu: **NOT VERIFIED**. P2.1 yüzeyinde endpoint/UI yok; `pnpm test:e2e` bunu açıkça deferred bildirir.
- Deterministik seed: **NOT VERIFIED / uygulanamaz**. Bu ledger mutasyonu RNG kullanmaz.
- Sabit-seed Monte Carlo, 7/30 günlük ekonomi, performans yük testi, browser matrisi ve erişilebilirlik: **NOT VERIFIED**. Bunlar P2.3--P2.7, UI veya release kapsamındadır; bu dar task-smoke’a ait değildir.

## Kapı sonucu

`PASS`
