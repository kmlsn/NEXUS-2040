# P2.4 Yaşam Döngüsü Test Raporu

## Kimlik

| Alan | Değer |
|---|---|
| Mod | `task-smoke` |
| Faz/görev | Faz 2 / `P2.4` — 24–36 saatlik lazy accrual ve tesis depolama penceresi |
| Commit/build | `10e88e904ce877af8a3656fdc0af739c68d5071e` üzerinde kaydedilmemiş P2.4 çalışma ağacı |
| Formül/içerik sürümü | `balance-1.2` / `asteria-baseline-0.2` |
| Ortam | Windows `10.0.26200.0`; Node `v24.16.0`; pnpm `11.19.0`; yerel PostgreSQL 17.7 ve Redis 7.4.3 Compose servisleri |
| Seed/fixture | RNG kullanılmaz. Sabit sunucu zamanı `1800000000000` ms, L1 tesisler ve ayrı disposable PostgreSQL veritabanı `nexus_p24_lifecycle`. |
| Tarih ve ajan | 2026-08-14, `lifecycle_game_tester` |

## Kapsam ve yolculuklar

| Yolculuk/invariant | Beklenen gözlenebilir davranış | Sonuç | Kanıt |
|---|---|---|---|
| Yeni profil | Tesis satırı olmayan profil settlement çağrısında kaynak/ledger hareketi üretmez. | PASS | Bağımsız PostgreSQL probe: `settled=false`, `elapsedMs=0`, boş delta. |
| Geri dönen profil ve depolama sınırı | 30 saat sonra L1 mikro şebeke + veri merkezi yalnız 24 saatlik üretimi alır: Enerji `+480000000`, İşlem Gücü `+3600000000` mikro-birim. | PASS | Probe, `elapsedMs=86400000` ve beklenen iki ledger deltasıyla geçti. |
| Cursor ve tekrar deneme | Aynı `asOf` ikinci kez talep edildiğinde yeni kredi yoktur; bir saat sonraki talep yalnız o saatin Enerji `+20000000`, İşlem Gücü `+150000000` çıktısını verir. | PASS | Aynı-cursor çağrısı boş sonuç; sonraki çağrı tam bir saatlik delta verdi. |
| Kesirli kısmi verim taşıması | Mikro şebeke + veri merkezi + kısmi laboratuvarın 3 saat + 6 saatlik ardışık settlement’ı, 9 saatlik tam kesir toplamına eşittir. | PASS | Ardışık uzmanlık kredileri `10666666` ve `21333334`; kalıcı toplam `32000000` mikro-birim. |
| Enerjisiz tesis | Enerji kaynağı olmayan veri merkezi çıktı veya tüketim üretmez ve geriye dönük claim avantajını engellemek için cursor talep zamanına ilerler. | PASS | Boş delta/`settled=false`; DB cursor’u `asOf` değerine ilerledi. |
| Kesinti ve toparlanma | Settlement içindeki geçersiz kalıcılık taşıması hata verdiğinde hiçbir tesis cursor’u veya ledger girdisi commit edilmez; durum onarıldıktan sonra aynı aralık bir kez tam settlement olur. | PASS | Zorlanmış, şema-geçerli fakat oranla uyumsuz taşıma: hata sonrası iki cursor başlangıçta ve ledger entry sayısı `0`; normal taşıma geri yüklendikten sonra `+20000000` Enerji / `+150000000` İşlem Gücü, aynı `asOf` tekrarında kredi yok. |
| Migration/geri dönüş koruması | Lazy-accrual durumuyla geri alma reddedilir; boş durumla 007 migration geri alınır. | PASS | API entegrasyon paketi geçti. |
| Dar kritik regresyon | Saf accrual/enerji matematiği, TypeScript denetimi, mevcut ledger idempotency/eşzamanlılık kontrolleri geçer. | PASS | Simulation typecheck/test ve API integration exit `0`. |

## Komutlar ve ham sonuçlar

| Komut | Exit code | Özet |
|---|---:|---|
| `pnpm --filter @nexus/simulation run typecheck` | 0 | Accrual dahil simülasyon TypeScript denetimi geçti. |
| `pnpm --filter @nexus/simulation run test` | 0 | PCG32, enerji ve ortak fixture regresyonları ile `exact lazy-accrual carry and storage cap` geçti. |
| `pnpm services:up; pnpm --filter @nexus/api run test:integration; pnpm services:down` | 0 | 007 migration geri alma koruması, ledger idempotency/yarış koruması ve atomik lazy-accrual integration yolu geçti. |
| `pnpm services:up; pnpm services:check; <stdin üzerinden bağımsız TypeScript/PostgreSQL P2.4 probe>; pnpm services:down` | 0 | Yeni/geri dönen profil, sınır, cursor, taşıma, enerjisiz tesis, hata sonrası rollback ve recovery yolculukları geçti; disposable test veritabanı sonunda silindi. |
| İlk disposable runner çağrısı | 1 | API çalışma dizininde kök `services:up` komutu aranmıştı; ürün kodu veya veri çalıştırılmadı/değiştirilmedi. |
| İkinci disposable runner çağrısı | 1 | Compose hazır bildiriminden hemen sonra yerel TCP bağlantısı reddedildi; test veritabanı oluşturulmadan durdu. Sonraki servis sağlığı kontrolü ve aynı probe geçti. |
| Yardımcı tek satır runner çağrısı | 1 | `tsx --eval` CJS modunda top-level `await` kabul etmedi; uygulama akışı çalıştırılmadı. Sonraki stdin runner IIFE ile geçti. |

Bağımsız runner yalnız kurmaca tesis türleri, sabit zamanlar ve mikro-birimler kullandı; ağ hedefi, siber operasyon içeriği, kalıcı profil veya ekran görüntüsü üretmedi.

## Bulgular ve regresyonlar

| Kimlik | Şiddet | Yeniden üretim | Beklenen/gerçek | Durum |
|---|---|---|---|---|
| Yok | — | — | P2.4 oyuncu yolunda P0/P1 kusuru gözlenmedi. | Kapalı |

## Tarihsel rate-keyed yeniden test (yerine canonical-carry geçti)

P2.4 çalışma ağacına eklenen final-window chronology ve rate-keyed carry düzeltmelerinden sonra bu `task-smoke` yeniden çalıştırıldı. Aynı ortamda aşağıdaki ek bağımsız PostgreSQL probe’u exit `0` ile geçti:

| Yolculuk/invariant | Beklenen gözlenebilir davranış | Sonuç | Kanıt |
|---|---|---|---|
| Eşit olmayan depolama pencereleri | 36 saattir talep edilmemiş L9 Veri Merkezi, 24 saatlik L1 Mikro Şebeke penceresi açılmadan önce üretmez; yalnız son 24 saatteki kısmi Enerji tahsisiyle tam kesir hesabı kadar İşlem Gücü üretir. İki Enerji hareketi birbirini sıfırladığı için net Enerji ledger deltası yoktur. | PASS | Dinamik katalog oranıyla hesaplanan `floor(L9_output * 90_000_000 * 24 / L9_demand)` ile settlement `compute` deltası birebir eşleşti; iki cursor `asOf` değerine ilerledi; aynı `asOf` tekrarında kredi yoktu. |
| Rate değişimi ve rate-keyed carry | Bankadaki Enerji sırasıyla `90M → 60M → 90M` olduğunda Veri Merkezi tam/kısmi/tam, Laboratuvar kısmi/sıfır/kısmi verimde çalışır. Önceki kısmi carry, farklı oranlı orta pencerede bozulmadan saklanır ve aynı oran döndüğünde devam eder. | PASS | Son bakiyeler Enerji `0`, İşlem Gücü `428571428`, Uzmanlık `7111111` mikro-birim; Veri Merkezi `900000000/7` hız anahtarı için carry `14400000/25200000` olarak kalıcıydı. |

| Ek komut | Exit code | Özet |
|---|---:|---|
| `pnpm --filter @nexus/simulation run typecheck && pnpm --filter @nexus/simulation run test && pnpm services:up && pnpm --filter @nexus/api run test:integration && pnpm services:down` | 0 | Saf accrual/enerji, migration ve API ledger/accrual integration regresyonları yeniden geçti. |
| `pnpm services:up; pnpm services:check; <stdin bağımsız unequal-window/rate-keyed PostgreSQL probe>; pnpm services:down` | 0 | L1/L9 final-window kronolojisi, cursor replay’i, tam/kısmi/sıfır oran geçişleri ve rate-keyed carry kalıcılığı geçti; disposable veritabanı silindi. |

## Canonical-carry son yeniden testi

Rate-keyed taşıma tasarımı, mevcut çalışma ağacında canonical carry ile değiştirildi. Bu `task-smoke`, güncel sunucu davranışını ayrı disposable PostgreSQL veritabanında yeniden doğruladı:

| Yolculuk/invariant | Beklenen gözlenebilir davranış | Sonuç | Kanıt |
|---|---|---|---|
| Yeni profil | Tesis satırı olmayan profil kaynak ya da ledger hareketi üretmez. | PASS | Settlement boş delta ve `settled=false` döndü. |
| Final-window depolama zaman çizelgesi | 36 saat eski L9 Veri Merkezi, 24 saat pencereli L1 Mikro Şebeke aktifleşmeden üretmez; yalnız son 24 saatin kısmi Enerji tahsisi kadar İşlem Gücü yazar. | PASS | `elapsedMs=36h`; `compute=floor(L9_output * 90_000_000 * 24 / L9_demand)`, net Enerji deltası yok, iki cursor `asOf`a ilerledi. |
| Retry/idempotence | Aynı settlement zamanı ikinci kez kredi oluşturmaz. | PASS | Hem unequal-window hem canonical-rate profillerinde ikinci `asOf` çağrısı `settled=false` ve boş delta verdi. |
| Canonical taşıma oran geçişi | Banka Enerjisi `90M → 60M → 90M` iken Veri Merkezi tam/kısmi/tam, Laboratuvar kısmi/sıfır/kısmi verimde çalışır; tek canonical kesir oran sınırında korunur. | PASS | Son bakiyeler Enerji `0`, İşlem Gücü `428571428`, Uzmanlık `7111111` mikro-birim. Kalıcı output carry: Veri Merkezi `4/7`, Laboratuvar `1/9`. |
| Geçersiz istek kurtarma | Negatif sunucu zamanı reddedilir ve mevcut carry/cursor değişmez. | PASS | Hata sonrası Veri Merkezi carry’si `4/7` olarak korundu. |
| Regresyon rotası | Simulation typecheck/test, P2.4 fixture ve API migration/ledger integration geçer. | PASS | Aşağıdaki iki komut exit `0` verdi. |

| Ek komut | Exit code | Özet |
|---|---:|---|
| `pnpm --filter @nexus/simulation run typecheck && pnpm --filter @nexus/simulation run test && pnpm services:up && pnpm --filter @nexus/api run test:integration && pnpm services:down` | 0 | Canonical P2.4 golden fixture, saf simülasyon, 007 migration, ledger idempotency/eşzamanlılık ve lazy-accrual integration geçti. |
| `pnpm services:up; pnpm services:check; <stdin canonical-carry lifecycle PostgreSQL probe>; pnpm services:down` | 0 | Yeni profil, L1/L9 storage chronology, aynı-zaman retry’i, tam/kısmi/sıfır/tam enerji geçişi, canonical carry kalıcılığı ve geçersiz zaman kurtarması geçti; disposable veritabanı silindi. |

## Atlanan veya kullanılamayan kontroller

- Farklı tesis seviyesi/depolama eğrilerinin tüm kombinasyonları ve 6 saat/queue-konfigürasyon sınırlarının gerçek inşa kuyruğu ile ayrıştırılması: **NOT VERIFIED / P2.5 kapsamı**. P2.4’te inşa kuyruğu ya da seviyeyi değiştiren oyuncu akışı yoktur.
- UI, klavye, odak, renk bağımsız anlam ve reduced-motion: **NOT VERIFIED / P2.6 ve Faz 7 kapsamı**; bu görevde oyuncuya sunulan ekran veya endpoint yoktur.
- Tarayıcı E2E, performans benchmarkı, uzun dönem ekonomi denge karşılaştırması ve release dayanıklılığı: **NOT VERIFIED / gelecek fazlar (P2.7, Faz 8–10)**.
- Gerçek zamanlı eşzamanlı iki lazy-accrual settlement talebinin yarış testi: **NOT VERIFIED / P2.8 kapsamı**. Mevcut görevin tek settlement ve ledger içi atomiklik/idempotency entegrasyon yolu test edildi.

## Kapı sonucu

`PASS`
