# P3.2 Lifecycle Test Report — NPC organizations and profile relationships

## Result

`PASS`

## Test identity

| Field | Value |
|---|---|
| Mode | `task-smoke` |
| Phase/task | Phase 3 / `P3.2` — three versioned NPC organization definitions, server-persisted organization state, and neutral per-profile relationship initialization |
| Commit/build | `b1a1a29` plus uncommitted P3.2 work on `codex/p3-2-npc-organizations` |
| Formula/content version | `balance-1.2` / `asteria-baseline-0.2` |
| Environment | Windows 11; Node `v24.16.0`; pnpm `11.19.0`; Docker PostgreSQL/Redis on configured local loopback ports |
| Seed/fixture | D-028 baseline: Nexilune `centralize/65`, Civic Grid `continuity/60`, Free Mesh `distribute/55`; disposable `nexus_migration_test` PostgreSQL database |
| Date/agent | `2026-08-16T21:10:09Z`; 013 integrity re-smoke / `lifecycle_game_tester` |

## Player-facing journeys and invariants checked

| Journey/invariant | Expected observable behavior | Result | Evidence |
|---|---|---|---|
| NPC catalogue baseline | Exactly three fictional organizations expose D-028 identities, doctrine/readiness values, bounded named capacity vectors, and reject an unknown organization id. | PASS | Content tests plus an independent executable D-028 identity/invalid-id assertion passed. |
| Fresh global NPC state | A newly migrated database has exactly three version-pinned organization-state rows. Out-of-range readiness and a non-catalogue doctrine are rejected by the database. | PASS | Disposable-database migration integration passed. |
| Legacy-profile migration recovery | A profile created before migration `013` receives its three neutral relationship records during `013` application. | PASS | Independent disposable-database re-smoke rolled back pristine `013`, created a legacy profile, reapplied it, and observed exactly three rows without invoking the application helper. |
| Fresh profile relationship bootstrap | A profile created after `013` automatically receives three neutral, signed-tenths relationship rows—one for each organization. | PASS | Independent disposable-database re-smoke inserted a fresh profile directly and observed exactly three rows from the database trigger, without invoking the application helper. |
| Returning/retry and concurrent initialization | Repeating initialization concurrently must leave exactly three rows, never six; a duplicate direct relationship insert is rejected. | PASS | Migration integration exercised two concurrent `ensureProfileNpcRelationships` calls and the duplicate primary-key path. |
| Interruption/recovery and persistence safety | Migration rollback is refused while bootstrap-created or otherwise persisted profile relationships exist; pristine rollback and re-application remain available before player state. | PASS | Independent re-smoke verified rollback refusal after both backfill and trigger bootstrap; migration integration also passed persisted-state rollback refusal, rollback, and re-apply paths. |
| Existing API/worker health regression | Existing API and worker health contracts, correlation/error behavior, and structured logs remain live. | PASS | `node scripts/http-smoke.mjs` passed after P3.2 migration checks. |
| Smallest critical regression | Existing API ledger unit and worker world-cycle unit contracts still pass. | PASS | API and worker unit runners passed. |

## Commands

| Command | Exit code | Summary |
|---|---:|---|
| `pnpm --filter @nexus/content run test` | 0 | Facility regression and P3.2 organization catalogue checks passed. |
| `pnpm --filter @nexus/content run typecheck` | 0 | Content package type check passed. |
| `pnpm --filter @nexus/content exec tsx -e "...D-028 exact values and invalid-id assertion..."` | 0 | Exact D-028 identity/doctrine/readiness tuples and invalid-id rejection passed. |
| `P3.2 disposable 013 re-smoke piped to tsx` | 0 | Created a pre-013 legacy profile, reapplied 013 to verify backfill, created a post-013 profile to verify automatic trigger bootstrap, and verified rollback refusal with persisted rows. |
| `pnpm run services:check` | 0 | Local PostgreSQL and Redis were healthy. |
| `pnpm --filter @nexus/api exec tsx --env-file-if-exists=../../.env scripts/migrations-integration.ts` | 0 | Fresh state, schema constraints, concurrent relationship initialization, rollback/reapply, and prior migration guards passed in a disposable database. |
| `pnpm --filter @nexus/api run test` | 0 | Existing ledger micro-unit/bigint unit contract passed. |
| `pnpm --filter @nexus/worker run test` | 0 | Existing forward-only world-cycle unit contract passed. |
| `pnpm --filter @nexus/worker run typecheck` | 0 | Worker type check passed. |
| `node scripts/http-smoke.mjs` | 0 | API/worker health, correlation, public-error, and structured-log contracts passed. |
| `pnpm run plan:check` | 0 | `11` phases, `83` tasks, `26` completed tasks, and `28` decisions validated. |
| `git diff --check` | 0 | No whitespace-error output; only CRLF conversion warnings from Git. |

## Defects and regressions

No P0 or P1 defect was observed.

## Explicitly not verified

- P3.2 does not expose a player-facing NPC screen or profile-creation HTTP endpoint. Browser accessibility, authenticated profile creation, and user-entered relationship updates are therefore **NOT VERIFIED** and are not required by this server-persistence task.
- P3.3–P3.8 behavior remains **NOT VERIFIED**: NPC decision cycles/adaptation, market shocks, contracts, event log, 120-day deterministic stress, balance, performance, backup/restore, telemetry, and release compatibility.
- This is a task smoke, not a Phase 3 regression or release-acceptance run.

## Gate result

`PASS`
