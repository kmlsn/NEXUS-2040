# P1.8 Yaşam Döngüsü Test Raporu

## Kimlik

| Alan | Değer |
|---|---|
| Mod | `task-smoke` |
| Faz/görev/sürüm | `P1.8` |
| Commit/build | `63c2c49` + P1.8 çalışma ağacı değişiklikleri |
| Formül/içerik sürümü | `balance-1.2` / `asteria-baseline-0.2` |
| Ortam | Windows, Node `v24.16.0`, pnpm `11.19.0`, Docker `29.6.2` |
| Seed/fixture | PCG32 ortak fixture; HTTP smoke request-ID contract |
| Tarih ve ajan | 2026-08-14, `lifecycle_game_tester` |

## Kapsam ve yolculuklar

| Yolculuk/invariant | Beklenen | Sonuç | Kanıt |
|---|---|---|---|
| Varsayılan yerel başlangıç | `.env` yokken servisler ayağa kalkar, migration tekrar çalıştırılabilir ve sağlık denetimi geçer. | PASS | `pnpm services:up`, `pnpm --filter @nexus/api run migrate`, `pnpm services:check` exit 0 |
| Kalite komut sözleşmesi | README'nin işaret ettiği `pnpm verify` tüm mevcut zorunlu kapıları yürütür. | PASS | `pnpm verify` exit 0; lint, typecheck, unit/fixture/HTTP smoke, migration integration, deferred E2E/balance ve plan kontrolü geçti. |
| HTTP liveness | API/worker health, request correlation, güvenli hata ve yapılandırılmış log sözleşmeleri korunur. | PASS | `pnpm verify` içindeki `scripts/http-smoke.mjs` PASS |
| Kapanış | `services:down` konteyner ve ağı kaldırır; volume'lar silinmez. | PASS | `pnpm services:down` exit 0; sonra `docker compose ... ps` boş. |
| İsteğe bağlı `.env` override | Özel portlar için Compose, migration ve integration aynı kök `.env` dosyasını kullanır. | PASS | Ana ajan bağımsız olarak `25432`/`26379` override ile migration + integration doğruladı; lifecycle ajanı varsayılan `.env`-siz yolu tekrar yürüttü. |

## Komutlar

| Komut | Exit code | Özet |
|---|---:|---|
| `pnpm services:up` | 0 | PostgreSQL ve Redis healthy. |
| `pnpm --filter @nexus/api run migrate` | 0 | `Applied: none`; tekrar çalıştırma güvenli. |
| `pnpm services:check` | 0 | PostgreSQL/Redis health kontrolü geçti. |
| `pnpm verify` | 0 | Tüm mevcut zorunlu kalite kapıları geçti. |
| `pnpm test` | 0 | PCG32 fixture ve API/worker HTTP smoke geçti. |
| `pnpm services:down` | 0 | Konteynerler ve ağ temiz kapandı. |

## Bulgular ve regresyonlar

| Kimlik | Şiddet | Yeniden üretim | Beklenen/gerçek | Durum |
|---|---|---|---|---|
| — | — | — | Açık P1.8 yaşam döngüsü regresyonu bulunmadı. | Kapalı |

## Atlanan veya kullanılamayan kontroller

- Web Vite sürecinin görsel tarayıcı denetimi yapılmadı; P1.8 kullanıcı akışı eklemedi. API/worker HTTP sözleşmesi mevcut smoke içinde doğrulandı.
- `test:e2e` ve `balance:check` planın bu safhasında açıkça `DEFERRED` no-op'tur; bu durum `pnpm verify` çıktısında görünür ve PASS iddiasına gizlenmemiştir.

## Kapı sonucu

`PASS`
