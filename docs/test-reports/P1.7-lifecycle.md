# P1.7 Yaşam Döngüsü Test Raporu

## Kimlik

| Alan | Değer |
|---|---|
| Mod | `task-smoke` |
| Faz/görev | Faz 1 / `P1.7` — CI üzerinde lint, typecheck, unit ve entegrasyon kapıları |
| Tarih | 2026-08-14 |
| Ortam | Windows, Node.js `v24.16.0`, pnpm `11.19.0`, Docker Compose |
| Commit/build | Depoda henüz commit yok (`NO_COMMIT`); denetim çalışma ağacındaki P1.7 workflow’u üzerinde yapıldı. |
| Kapsam dışı | Uzak GitHub Actions çalıştırması, e2e ve denge paketleri bu görevin CI zorunlu-kapı kapsamı dışındadır. |

## Kapsam ve yolculuklar

| Yolculuk/invariant | Beklenen | Sonuç | Kanıt |
|---|---|---|---|
| CI workflow sözleşmesi | Pull request üzerinde ayrı lint, typecheck, unit ve integration job’ları; her biri frozen install kullanır. | PASS | `pnpm exec node scripts/ci-contract-check.mjs` |
| Lint ve typecheck yerel karşılığı | CI komutları başarısız olmadan tamamlanır. | PASS | `pnpm lint`; `pnpm typecheck` |
| Unit kapısı | `pnpm test`, P1.2-P1.6 sözleşmelerini ve HTTP smoke’u tamamlar. | PASS | Üç ardışık `pnpm test` çalıştırması |
| Entegrasyon kapısı | Sağlıklı PostgreSQL/Redis üzerinde migration, sürüm sabitleme, idempotency ve ledger değişmezleri doğrulanır. | PASS | `pnpm services:up`; `pnpm services:check`; `pnpm test:integration` |
| Başarısızlık/temizlik yolu | İş akışı failure durumunda servis logunu alır, her durumda servisleri kapatır; yerelde kapanış sonrası container kalmaz. | PASS | Workflow `if: failure()` / `if: always()` adımları; `pnpm services:down`; kapanış sonrası `docker compose ... ps` boş çıktı |

## Komutlar

| Komut | Exit code | Özet |
|---|---:|---|
| `pnpm exec node scripts/ci-contract-check.mjs` | 0 | Workflow dört zorunlu kapıyı ve frozen kurulumu içeriyor. |
| `pnpm lint` | 0 | ESLint, uyarısız tamamlandı. |
| `pnpm typecheck` | 0 | Yedi workspace paketi TypeScript kontrolünden geçti. |
| `pnpm test` (3 ardışık tur) | 0 | Workspace, CI, Compose config, runtime-port, PCG32 ve HTTP smoke kontrolleri üç turda geçti. |
| `pnpm services:up` | 0 | PostgreSQL `17.7-alpine` ve Redis `7.4.3-alpine` healthy oldu. |
| `pnpm services:check` | 0 | Her iki yerel servis sağlıklı doğrulandı. |
| `pnpm test:integration` | 0 | Migration ileri/geri, idempotency, immutable ledger ve rollback koruması geçti. |
| `pnpm services:down` | 0 | Container ve ağ kaldırıldı; volume’lar korunarak servis listesi boş kaldı. |

## Bulgular ve regresyonlar

| Kimlik | Şiddet | Yeniden üretim | Beklenen/gerçek | Durum |
|---|---|---|---|---|
| LCT-P1.7-01 | P2 (izleme) | İlk birleşik lint/typecheck/test turunda HTTP smoke’un 100 ms log bekleme assertion’ı bir kez başarısız oldu; ardından doğrudan iki ve `pnpm test` ile üç ardışık tur geçti. | Beklenen: korelasyon logu görünür olmalı. Gerçek: beş tekrarın tümü geçti; kalıcı regresyon doğrulanamadı. | Kapalı — görev kapısını engelleyen P0/P1 bulgusu yok; gelecekte tekrar görülürse P1.6 smoke zamanlaması incelenmeli. |

## Atlanan veya kullanılamayan kontroller

- Uzak GitHub Actions çalıştırması: Bu yerel test ortamında GitHub’a push veya pull request oluşturulmadığından `NOT VERIFIED`. Workflow’un statik sözleşmesi ve aynı komutların yerel karşılığı doğrulandı.
- `test:e2e` ve `balance:check`: P1.7’nin CI zorunlu kapı setinde değil; `NOT VERIFIED` ve bu smoke sonucunun kapsamı dışındadır.

## Kapı sonucu

`PASS`
