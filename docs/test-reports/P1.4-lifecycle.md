# P1.4 Yaşam Döngüsü Test Raporu

## Kimlik

| Alan | Değer |
|---|---|
| Mod | `task-smoke` |
| Faz/görev/sürüm | Faz 1 / `P1.4` / profil, sürüm, idempotency ve kaynak ledger migration'ları |
| Commit/build | Commit yok; çalışma ağacı izlenmeyen proje dosyaları içeriyor |
| Formül/içerik sürümü | `balance-1.2` / `asteria-baseline-0.2` |
| Ortam | Windows, Node.js `24.x`, pnpm `11.16.0`, Docker Compose; PostgreSQL `17.7-alpine`, Redis `7.4.3-alpine` |
| Seed/fixture | İzole `nexus_migration_test` veritabanı; rastgele UUID profil/işlem kimlikleri; test sonunda silindi |
| Tarih ve ajan | 2026-08-10 UTC, `lifecycle_game_tester` |

## Kapsam ve yolculuklar

| Yolculuk/invariant | Beklenen | Sonuç | Kanıt |
|---|---|---|---|
| Yerel bağımlılıkları yeniden kullanma | PostgreSQL ve Redis, tekrar edilen smoke için healthy olmalı | `PASS` | `pnpm run services:up` exit `0`; iki Compose healthcheck'i yeşil. |
| Boş geçici DB ileri/geri/yeniden ileri akışı | İki migration boş geçici veritabanında uygulanmalı, ikisi geri alınmalı; ardından iki kez ileri çalıştırma güvenle tekrarlanmalı | `PASS` | `pnpm run test:integration` exit `0`; migration integration runner'ın ileri → geri → geri → ileri → ileri yolculuğu tamamlandı. |
| İçerik/formül sürüm bağlama | Yeni profil yalnız tanımlı içerik/formül çiftiyle oluşturulabilmeli | `PASS` | Aynı integration runner `asteria-baseline-0.2` ve `balance-1.2` ile profil oluşturdu ve `PASS` sonucu verdi. |
| Aynı idempotency anahtarının reddi | Aynı profil/scope/anahtar için ikinci istek kaydı kabul edilmemeli | `PASS` | Integration runner ikinci `operation` / `same-key` kaydının DB benzersizlik kuralıyla reddedildiğini doğruladı. |
| Ledger değişmezliği | Yazılmış kaynak ledger girdisi güncellenememeli, silinememeli veya topluca boşaltılamamalı | `PASS` | Bağımsız tekrar smoke'unda integration runner, inserted `capital` girdisi için hem `DELETE` hem de `TRUNCATE resource_ledger_entries` girişiminin trigger tarafından reddedildiğini doğruladı. Önceki kalite bulgusu kapandı. |
| Geri alma veri koruması | Kalıcı ledger verisi varken en son migration geri alınamamalı | `PASS` | Integration runner, kalıcı işlem ve ledger girdisinden sonra rollback isteğinin reddedildiğini doğruladı. |
| Mikro-birim girdi sınırı | Sıfır ve kesirli tutarlar kabul edilmemeli | `PASS` | Integration runner `0` ve `1.5` değerlerinin parser tarafından reddedildiğini doğruladı. |
| Smoke temizliği | Geçici migration DB'si iş bittiğinde kalmamalı | `PASS` | PostgreSQL katalog sorgusu `nexus_migration_test` için `absent` döndürdü. |

## Komutlar

| Komut | Exit code | Özet |
|---|---:|---|
| `pnpm run services:up` | 0 | PostgreSQL ve Redis çalışır/healthy duruma geldi. |
| `pnpm run test:integration` | 0 | Servis healthcheck'i ile migration, idempotency, immutable ledger ve rollback guard kontrolleri geçti. |
| `docker compose -f infra/docker-compose.yml exec -T postgres psql -U nexus_local -d postgres -Atc "SELECT ... pg_database ... nexus_migration_test ..."` | 0 | Geçici test veritabanı `absent` bulundu. |
| `pnpm run test` | 0 | Workspace ve yerel servis yapılandırması sözleşmesi geçti. |
| `pnpm run typecheck` | 0 | Yedi TypeScript workspace paketi tip kontrolünden geçti. |
| `pnpm run services:up` ve `pnpm run test:integration` (tekrar smoke) | 0 | `TRUNCATE` koruması eklendikten sonra migration akışı ve tüm P1.4 kritik invariant'ları tekrar geçti. |

## Bulgular ve regresyonlar

| Kimlik | Şiddet | Yeniden üretim | Beklenen/gerçek | Durum |
|---|---|---|---|---|
| Yok | — | — | P0/P1 bulgusu gözlenmedi. | Kapalı |

## Atlanan veya kullanılamayan kontroller

- Eşzamanlı çoklu istemci yarışı ve yüksek hacimli tekrar teslim testi `NOT VERIFIED`: P1.4 integration testi tek süreçli idempotency/immutability/rollback korumalarını kapsar; dayanıklılık benchmarkı Faz 8 kapsamındadır.
- Oyuncu profili oluşturma HTTP yolculuğu, API health endpoint'i ve tarayıcı E2E `NOT VERIFIED`: ilgili API yüzeyi P1.6, oyuncu akışları ise sonraki oyun fazları kapsamındadır.
- Deterministik saat/UUID/PCG32 golden vektörü `NOT VERIFIED`: P1.5 kapsamındadır.

## Kapı sonucu

`PASS`
