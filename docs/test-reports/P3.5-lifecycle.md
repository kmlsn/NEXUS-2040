# P3.5 Lifecycle Test Report — deterministic market-offer collateral settlement

## Result

`PASS`

## Test identity

| Field | Value |
|---|---|
| Mode | `task-smoke` |
| Phase/task | Phase 3 / `P3.5` — server-authoritative market-contract snapshot, deterministic offer result, and bounded collateral settlement |
| Commit/build | `3b65136` (`codex/p3-5-bid-collateral`) |
| Formula/content version | `balance-1.2` / `asteria-baseline-0.2` |
| Environment | Windows 11; Node `v24.16.0`; pnpm `11.19.0`; local Docker PostgreSQL and Redis |
| Seed/fixture | `20260809`; `packages/simulation/fixtures/contract-offer-v1.json`; stream `contract:fixture:offer:profile:award` |
| Date/agent | `2026-08-17T09:30:00Z`; `lifecycle_game_tester` |

This is a narrow task-smoke result. Phase 3 remains `ACTIVE`; this is neither the Phase 3 regression gate nor release acceptance.

The smoke was rerun after the P3.5 shared fixture gained its half-away `price_score` boundary vector (`bid_micro=603`, `fair_value_micro=857600000`, scaled score `70313`). The current working tree is the tested build; the implementation commit remains `3b65136` and the fixture/test/model verifier changes are pending commit.

## Player-facing journeys and invariants checked

| Journey/invariant | Expected observable behavior | Result | Evidence |
|---|---|---|---|
| Fresh profile, server-derived offer | A fresh profile with liquid capital can receive one market snapshot/offer whose market value, score inputs, seed stream, hold, result, and refund are derived and persisted by the server/database. | PASS | `market-contract-integration.ts` created a fresh database/profile, funded it via the authoritative ledger, created a snapshot, and settled the offer. It also rejected a direct snapshot insert whose stored fair value disagreed with the basis-point formula. |
| Deterministic seed and cross-runtime replay | The fixed score, probability threshold, PCG draw, and award decision match the versioned fixture in both TypeScript and Python. | PASS | `pnpm --filter @nexus/simulation run test` and `py -3 tools/verify_contract_offer_fixture.py` both passed. |
| Half-away price-score boundary | A price score exactly around the ninth-decimal half-away boundary is preserved as the same integer-scaled value in TypeScript and Python. | PASS | The new shared vector (`603` / `857600000` → `70313 / 1e9`) passed in the simulation suite, Python verifier, and root fixture-check runner. |
| Returning profile / retry | Retrying the same profile/contract/bid returns the existing terminal settlement, rather than creating another hold or refund. A changed bid is rejected. | PASS | `market-contract-integration.ts` verified replay identity and refund transaction identity, then verified bid-mismatch rejection. |
| Invalid input and loss-safe boundary | Non-canonical bid strings are rejected; collateral below the four-micro minimum cannot use the 75% loss path; valid held collateral is ledger-linked with exactly the award/loss refund. | PASS | Integration test rejected leading-zero, signed, and whitespace variants and checked `held_micro >= 4`, reason codes, and exact net capital. Simulation tests checked the two-micro cap, four-micro minimum, and three-micro loss refund boundary. |
| Single-settlement competition / interruption recovery | Two independent database pools racing for one market contract yield exactly one terminal offer; the other attempt is unavailable, with no duplicate offer row. | PASS | `market-contract-integration.ts` used concurrent pools and checked one fulfilled/one rejected result plus exactly one persisted offer. |
| Persistence and migration recovery | The new schema rolls back when pristine, applies again, and refuses rollback after snapshot/offer history exists. Existing migration/idempotency/immutable-ledger behavior remains valid. | PASS | P3.5 integration exercised pristine down/up and persisted-history rollback rejection. `migrations-integration.ts` passed version pinning, idempotency, immutable-ledger, and rollback guards. |
| Scope and authority boundary | P3.5 adds no player-facing board, campaign/target/operation, expiry/archive, failure-analysis resource reward, or externally supplied result/seed/score. | PASS | Repository scope inspection found the new contract service and migrations only; no API route/controller for market offers. D-031 boundary was validated by `pnpm run plan:check`. |
| Small critical regression set | The worker's existing deterministic world-cycle locking adapter remains valid. | PASS | `pnpm --filter @nexus/worker run test` passed its forward-only deterministic state test. |

## Commands

| Command | Exit code | Summary |
|---|---:|---|
| `pnpm run services:up` | 0 | Started local PostgreSQL and Redis; both became healthy. |
| `pnpm run services:check` | 0 | PostgreSQL and Redis health checks passed. |
| `pnpm --filter @nexus/simulation run test` | 0 | Contract-offer score, collateral, deterministic award, plus pre-existing simulation suites passed. |
| `py -3 tools/verify_contract_offer_fixture.py` | 0 | Python fixture matched score, threshold, PCG draw, and award. |
| `node scripts/contract-offer-fixture-check.mjs` | 0 | Root fixture-check runner accepted the updated shared TypeScript/Python contract-offer vector. |
| `pnpm --filter @nexus/api run typecheck` | 0 | API type check passed. |
| `pnpm --filter @nexus/api exec tsx --env-file-if-exists=../../.env scripts/market-contract-integration.ts` | 0 | Fresh snapshot, invalid fair-value defense, replay, invalid bids, ledger conservation, competitor rejection, race, and rollback recovery passed. |
| `pnpm --filter @nexus/api exec tsx --env-file-if-exists=../../.env scripts/migrations-integration.ts` | 0 | Existing migration/version/idempotency/immutability/rollback regression passed. |
| `pnpm --filter @nexus/worker run test` | 0 | Forward-only deterministic world-cycle locking adapter passed. |
| `pnpm run plan:check` | 0 | Plan validation passed: 11 phases, 83 tasks, 29 completed tasks, 31 decisions. |
| `git diff --check` | 0 | No whitespace errors; unrelated pre-existing working-tree modifications were preserved. |

## Defects and regressions

No P0 or P1 defect was observed.

The first local-service probe returned exit code `1` because PostgreSQL was not running. This was a test-environment precondition, not a product defect; after `pnpm run services:up`, the same health check and all persistence tests passed.

## Explicitly not verified

- Browser/UI interaction, keyboard/accessibility, a contract board, listing/expiry/archive, campaign content, operation outcomes, failure-analysis reward, event log, and telemetry are **NOT VERIFIED** because D-031 assigns them to P3.6, P3.7, P4, P6, or later.
- Backup/restore, clean install, benchmark/performance, full root `pnpm verify`, E2E, Phase 3 regression, release acceptance, and post-release compatibility are **NOT VERIFIED** by this task-smoke mode.

## Gate result

`PASS`
