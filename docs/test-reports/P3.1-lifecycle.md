# P3.1 Lifecycle Test Report — versioned world state and deterministic cycle

## Result

`PASS`

## Test identity

| Field | Value |
|---|---|
| Mode | `task-smoke` |
| Phase/task | Phase 3 / `P3.1` — versioned singleton `world_state` and forward-only six-hour deterministic world cycle |
| Commit/build | `db88a2b` plus uncommitted P3.1 work including migrations `011_world_state` and `012_world_state_immutability`; branch `codex/p3-1-world-cycle` |
| Formula/content version | `balance-1.2` / `asteria-baseline-0.2` |
| Environment | Windows 11 Pro `10.0.26200`; Node `v24.16.0`; pnpm `11.19.0`; local Docker PostgreSQL/Redis services on the configured loopback ports |
| Seed/fixture | `master_seed=20260809`, epoch `1767225600000`, fixed five-cycle boundary fixture, transaction rollback fixture, and random disposable PostgreSQL database name |
| Date/agent | 2026-08-16 / `lifecycle_game_tester` |

## Player-facing journeys and invariants checked

| Journey/invariant | Expected observable behavior | Result | Evidence |
|---|---|---|---|
| Fresh global world state | Migration creates exactly one state record pinned to the normative content/formula versions, an unsigned-64 seed, and the fixed server epoch. A second state record or invalid version/seed/revision is rejected. | PASS | API migration integration applied both migrations, checked the singleton and rejected the invalid-state writes. |
| Six-hour boundary and deterministic replay | No cycle is completed before a full six-hour boundary; at the boundary one cycle completes; advancing once to five cycles equals five sequential advances; repeating the same time does not change state. | PASS | Simulation suite passed the exact boundary, one-vs-five-step, replay, invalid seed/version, and backward-clock cases. |
| Concurrent worker delivery | Two independent worker services receiving the same target time must produce one five-cycle advancement and one no-op, leaving the persisted state at `completed_cycles=5`, `state_revision=6`. | PASS | Worker PostgreSQL integration passed concurrent `FOR UPDATE` delivery against a disposable migrated database. |
| Interruption, transaction recovery, restart, and backward time | A rolled-back advancement leaves the state unchanged; a restarted worker and an earlier server timestamp cannot create or rewind cycles. | PASS | Worker integration rolled back a five-cycle transaction, then checked restart replay and one-cycle-earlier server time as no-ops. |
| Configuration and revision tamper resistance | Content/formula version, seed, and epoch are immutable after creation; completed cycles and state revision cannot move backward; migration rollback refuses after progression. | PASS | API migration integration rejected all listed tamper writes, verified pristine migration rollback/reapply, and rejected rollback plus backward state mutation after one completed cycle. |
| Worker liveness while checking the world | A worker process starts, runs its startup world-cycle check, and remains reachable through its health endpoint. | PASS | After applying `012`, a separate worker process logged `world_cycle_checked` with `advancedCycles=0`, `completedCycles=911`, `stateRevision=912`; `GET /health` returned `{"status":"ok","service":"worker",...}`. The dedicated listener was stopped after the test. |
| Smallest critical regression | The pre-existing migration/ledger/P2.7/P2.8 integration paths still pass in isolated reruns. | PASS | API migration integration passed; earlier isolated P2.7 and P2.8 runners passed. No P3.1 test changed client-supplied balance, clock, seed, or result authority. |

## Raw commands and results

| Command | Exit code | Raw outcome |
|---|---:|---|
| `pnpm --filter @nexus/simulation run test` | 0 | Existing PCG, energy, accrual checks and `world.test.ts` passed. |
| `pnpm --filter @nexus/worker run test` | 0 | Worker locking adapter passed forward-only/replay/backward-time behavior. |
| `pnpm --filter @nexus/worker run typecheck` | 0 | Worker type check passed. |
| `pnpm run services:check` | 0 | Local PostgreSQL and Redis containers healthy. |
| `pnpm --filter @nexus/api exec tsx --env-file-if-exists=../../.env scripts/migrations-integration.ts` | 0 | Migration singleton, immutability, revision, forward/down, and prior migration guards passed. |
| `pnpm --filter @nexus/worker run test:integration` | 0 | Disposable-database migration, concurrent worker, rollback, restart, and backward-clock checks passed. |
| `pnpm --filter @nexus/api run migrate` | 0 | Local environment applied `012_world_state_immutability`. |
| `cmd /c "set WORKER_PORT=3013&& pnpm --filter @nexus/worker run start"`; `GET http://127.0.0.1:3013/health` | 0 | Startup world-cycle event emitted, then health returned `worker/ok`; exact verified listener was stopped afterward. |
| `pnpm --filter @nexus/api exec tsx --env-file-if-exists=../../.env scripts/p2-7-ledger-balance-integration.ts` | 0 | P2.7 deterministic full-ledger comparison passed. |
| `pnpm --filter @nexus/api exec tsx --env-file-if-exists=../../.env scripts/p2-8-property-integration.ts` | 0 | P2.8 generated property integration passed, seed `541065224`, `16` cases. |

## Defects

No P0 or P1 defect observed.

An earlier overlapping tester invocation of the legacy complete integration command received a PostgreSQL connection-termination error while another disposable database run was cleaning up. It was not used as pass evidence. The same P2.7/P2.8 and P3.1 checks were rerun serially and passed; no P3.1 behavioral failure reproduced.

## Explicitly not verified

- No player UI, API action, or persisted player-profile path is part of P3.1; fresh/returning profile, invalid player input, and browser accessibility paths are not applicable to this technical global-state task.
- This is not a Phase 3 regression: NPC organization state, market shocks, contracts, event log, 120-day stress, browser matrix, performance/load, backup/restore, telemetry, release acceptance, and post-release compatibility remain **NOT VERIFIED** until their planned scopes.

## Gate result

`PASS`
