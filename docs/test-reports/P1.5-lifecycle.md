# P1.5 Yaşam Döngüsü Test Raporu

## Kimlik

| Alan | Değer |
|---|---|
| Mod | `task-smoke` |
| Faz/görev/sürüm | Faz 1 / `P1.5` / deterministik Clock, UUID ve PCG32 altyapısı |
| Commit/build | Commit yok; çalışma ağacı izlenmeyen proje dosyaları içeriyor |
| Formül/içerik sürümü | `balance-1.2` / `asteria-baseline-0.2` |
| Ortam | Windows, Node.js `v24.16.0`, pnpm `11.16.0`, Python `3.14.5` |
| Seed/fixture | `packages/simulation/fixtures/pcg32-v1.json`; master seed `20260809`; `golden`, `operation:test-17:success`, `operation:test-17:detection` stream'leri |
| Tarih ve ajan | 2026-08-10, `lifecycle_game_tester` |

## Kapsam ve yolculuklar

| Yolculuk/invariant | Beklenen | Sonuç | Kanıt |
|---|---|---|---|
| Test saati | `FixedClock`, kurulduğu milisaniye değerini değiştirmeden döndürmeli | `PASS` | `pnpm --filter @nexus/contracts run test` exit `0`; sabit `1726080900000` değeri doğrulandı. |
| Test UUID sırası ve hata yolu | `SequenceUuidGenerator` değerleri sırayla vermeli; tükendiğinde fail-closed hata vermeli | `PASS` | Aynı contracts testi `a` → `b` sırasını ve üçüncü çağrının hata verdiğini doğruladı. |
| Aynı seed/sürüm/stream tekrar üretimi | Ortak fixture'daki her uint32 dizisi TypeScript'te birebir tekrar üretilmeli | `PASS` | `pnpm --filter @nexus/simulation run test` exit `0`; üç fixture akışı golden değerlerle eşleşti. |
| Stream izolasyonu | `detection` stream'inin 100 çekilişi, yeniden kurulan `success` stream'inin ilk üç çıktısını değiştirmemeli | `PASS` | Aynı simulation testi iki bağımsız yeniden üretimi karşılaştırdı. |
| Olasılık ve RNG sınırları | Half-away yuvarlama/eşikler fixture ile eşleşmeli; `uniform` açık `(0,1)` aralığında kalmalı; 64-bit dışı seed reddedilmeli | `PASS` | Aynı simulation testi eşikleri, uniform sınırını ve out-of-range seed reddini doğruladı. |
| Python–TypeScript ortak fixture | Python referans hesaplaması, tam aynı canonical JSON değerlerini üretmeli | `PASS` | `py -3 tools/verify_pcg_fixture.py` exit `0`. |
| Kök test sözleşmesi | Sürüm/fixture ve runtime-port testleri mevcut workspace test girişinden erişilebilir kalmalı | `PASS` | `pnpm test` exit `0`; workspace/config kontrolü, contracts, simulation ve Python fixture doğrulaması geçti. |

## Komutlar

| Komut | Exit code | Özet |
|---|---:|---|
| `pnpm --filter @nexus/contracts run test` | 0 | Sabit saat, sıralı UUID ve UUID tükenme davranışı geçti. |
| `pnpm --filter @nexus/simulation run test` | 0 | PCG32 golden vektörleri, stream izolasyonu, threshold ve sınır testleri geçti. |
| `py -3 tools/verify_pcg_fixture.py` | 0 | Python referansı ortak fixture'ı doğruladı. |
| `pnpm test` | 0 | Kök test sözleşmesi aracılığıyla bütün P1.5 testleri tekrar geçti. |
| `pnpm typecheck` | 0 | Yedi TypeScript workspace paketi tip kontrolünden geçti. |

## Bulgular ve regresyonlar

| Kimlik | Şiddet | Yeniden üretim | Beklenen/gerçek | Durum |
|---|---|---|---|---|
| Yok | — | — | P0/P1 bulgusu gözlenmedi. | Kapalı |

## Atlanan veya kullanılamayan kontroller

- HTTP/API veya tarayıcı oyuncu yolculuğu `NOT VERIFIED`: P1.5 yalnız runtime test portları ve saf simülasyon deterministikliği ekler; health endpoint'i P1.6, oyun akışları sonraki fazlardadır.
- Büyük ölçekli sabit-seed Monte Carlo/property paketi `NOT VERIFIED`: PCG32 ve olasılık eşik sözleşmesi doğrulanmıştır; normatif operasyon formülleri ile Monte Carlo kapısı Faz 4 kapsamındadır.

## Kapı sonucu

`PASS`
