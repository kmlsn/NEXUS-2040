# P2.8 Lifecycle Test Report

## Result

`PASS`

## Test identity

| Field | Value |
|---|---|
| Mode | `task-smoke` |
| Phase/task | Phase 2 / `P2.8` — deterministic property checks for negative balances, concurrent spending, signed-64-bit bounds, and clock rollback |
| Commit/build | `809a524a3ede14854d295b436a133429af71c239` with uncommitted P2.8 implementation work; Node `v24.16.0`, pnpm `11.19.0`, Python `3.14.5` |
| Formula/content version | `balance-1.2` / `asteria-baseline-0.2`; P2.8 adds invariant coverage and the server-side rollback guard, not a new economy formula |
| Environment | Windows host; Docker Compose PostgreSQL 17.7 and Redis 7.4.3 on loopback `15432`/`16379` |
| Seed/fixture | Deterministic LCG seed `541065224` (`0x20400008`), 16 generated concurrent-underflow cases, 8 exact-balance concurrency cases, fixed UUIDs, and fixed clock values around `1800000000000` ms |
| Date/agent | 2026-08-14 / `lifecycle_game_tester` |

## Player-facing journeys and invariants checked

| Journey/invariant | Expected observable behavior | Result | Evidence |
|---|---|---|---|
| Fresh profile: invalid atomic spend and numeric boundary | A request that would take one resource negative, or would exceed signed 64-bit storage, must be rejected with no partial transaction, balance change, or ledger projection drift. | PASS | P2.8 integration granted a disposable profile, rejected `energy -101` paired with another delta and an over-maximum grant, then confirmed the transaction count and all five projected balances were unchanged and valid. |
| Generated concurrent spending | For each seeded overspend pair, exactly one intent commits; every resource projection remains non-negative and equals the immutable ledger total. | PASS | Deterministic integration passed all 16 generated cases with one fulfillment and one rejection per profile. |
| Exact boundary and retry/idempotency | Two distinct concurrent debits that exactly consume a balance must both complete and end at zero; identical concurrent intent must create one transaction and replay once. | PASS | All 8 exact-balance cases reached zero; the concurrent identical intent returned one transaction ID with exactly one replay. |
| Returning/persisted accrual state and clock rollback | A profile with recorded facility cursors must not receive output, create a ledger event, move a cursor backward, or mutate carry state when settlement time is equal or earlier. | PASS | Fixed two-facility fixture checked a staggered cursor, then retried at equal, one-millisecond-earlier, and zero timestamps. The stored cursor/carry snapshot and transaction count were byte-for-byte unchanged. |
| Overflowing accrual recovery | An accrual that would exceed a resource’s signed 64-bit balance must reject atomically and retain its prior cursor for a safe later retry. | PASS | A profile at the maximum energy balance rejected the next microgrid accrual; the cursor stayed at its original fixed timestamp and ledger projection remained valid. |
| Deterministic replay | The same fixed property fixture must reproduce its outcome without cross-run state. | PASS | Standalone P2.8 integration ran twice, both reporting `PASS: P2.8 property integration seed=541065224 cases=16.` |
| Critical regression and interruption/retry | Earlier migration, immutable-ledger, P2.7 balance, and P2.8 paths must pass as one suite after an interrupted disposable-DB attempt is retried. | PASS | Initial root integration attempt exited `1` with PostgreSQL `3D000` because `nexus_migration_test` had just been dropped. A clean service-health retry of the complete chain exited `0` and passed all four runners. |
| Cleanup and service/listener recovery | All disposable databases must be removed and temporary HTTP smoke listeners must not remain after tests. | PASS | PostgreSQL catalog had none of `nexus_migration_test`, `nexus_ledger_test`, `nexus_p27_balance_test`, or `nexus_p28_property_test`; no listeners remained on `3100`/`3101`. Only expected Compose ports `15432`/`16379` remained. |

## Raw commands and results

| Command | Exit code | Raw outcome |
|---|---:|---|
| `py -3 -B .agents/skills/verify-game-phase/scripts/validate_plan.py GAME_PLAN.md` | 0 | `PASS: 11 phases, 83 tasks, 24 completed tasks, 26 decisions validated` |
| `pnpm services:up; pnpm services:check; pnpm --filter @nexus/simulation run test; pnpm --filter @nexus/api run test` | 0 | PostgreSQL/Redis healthy; deterministic simulation/accrual property tests and ledger micro-unit/bigint-bound tests passed. |
| `pnpm --filter @nexus/api exec tsx --env-file-if-exists=../../.env scripts/p2-8-property-integration.ts` (twice) | 0 | Both independent runs passed with fixed seed `541065224` and `16` generated cases. |
| `pnpm test:integration` | 1 | Interrupted disposable-run evidence: PostgreSQL reported `3D000`, database `nexus_migration_test` did not exist and “seems to have just been dropped or renamed.” No behavioral assertion was evaluated as a pass in this run. |
| `pnpm services:check; pnpm test:integration` | 0 | Clean retry passed service health, migration/version rollback guards, five-resource atomic ledger regression, P2.7 full-ledger comparison, and P2.8 property integration. |
| `pnpm verify` | 0 | Lint, typecheck, unit/fixture checks, web/API/worker smoke, complete integration suite, deferred early-phase E2E, balance audit, and plan check all passed. |
| PostgreSQL temporary-DB catalog query plus `Get-NetTCPConnection` listener probe | 0 | All disposable DBs absent; no listeners on `3100`/`3101`; expected services only. |

## Defects

No P0 or P1 defect was observed. The first complete integration invocation was interrupted by a disposable-database disappearance; it did not reproduce after a service-health check and the clean complete retry. The successful retry is the gate evidence.

## Explicitly not verified

- A UI-visible clock error/recovery message is not applicable: P2.8 changes server persistence behavior only, and no player-facing UI was added.
- Browser matrix, manual accessibility traversal, performance/load benchmarks, backup/restore, release acceptance, and live telemetry remain **NOT VERIFIED**; they are outside this task-smoke and later-phase/release scope.
- Full phase regression is not this task’s mode. The P2.8-specific property path plus the smallest critical ledger/migration/P2.7 regression set were checked.

