# P3.4 Lifecycle Test Report — story and market contract visibility

## Result

`PASS`

## Test identity

| Field | Value |
|---|---|
| Mode | `task-smoke` |
| Phase/task | Phase 3 / `P3.4` — versioned `story` and `market` contract kinds with deterministic visibility separation |
| Commit/build | `bf96701` (`codex/p3-4-contract-visibility`) plus the uncommitted P3.4 content-only change set |
| Formula/content version | `balance-1.2` / `asteria-baseline-0.2`; P3.4 itself introduces no numerical formula or content instance |
| Environment | Windows 11; Node `v24.16.0`; pnpm `11.19.0` |
| Seed/fixture | No RNG, persistence, seed, or time input exists in the P3.4 pure visibility contract; the full 2×2 Boolean eligibility matrix was exercised independently for each of the two kinds (eight evaluations), plus an invalid kind. |
| Date/agent | `2026-08-16T22:04:51Z`; `lifecycle_game_tester` |

This is a task-smoke result. Phase 3 remains `ACTIVE`; it is neither phase-regression nor release acceptance.

## Player-facing journeys and invariants checked

| Journey/invariant | Expected observable behavior | Result | Evidence |
|---|---|---|---|
| Story visibility across its full 2×2 matrix | For every `storyEligible`/`marketListed` pair, story visibility equals only `storyEligible`; the conflicting market value has no effect. | PASS | `packages/content/tests/contracts.test.ts` executed all four Boolean pairs for `story`. |
| Market visibility across its full 2×2 matrix | For every `storyEligible`/`marketListed` pair, market visibility equals only `marketListed`; the conflicting story value has no effect. | PASS | Same test executed all four Boolean pairs for `market`. |
| Invalid input | An unknown runtime contract kind throws rather than silently selecting a visibility source. | PASS | Same contract test's invalid-kind assertion. |
| Determinism, interruption, and retry | Each of the eight matrix evaluations has no mutable state, seed, time, or side effect; visibility is wholly selected by kind and its declared server-derived eligibility input. | PASS | Source/scope inspection of `packages/content/src/contracts.ts` and the complete two-kind matrix test. |
| Existing world/market regression | P3.3 fixed-point market and deterministic six-hour world-cycle behavior stays unchanged. | PASS | Simulation suite below. |
| Existing worker forward-only regression | The worker's locked, forward-only world/market adapter remains valid. | PASS | Worker unit suite below. |
| Scope boundary | No contract instance, campaign A01-A06 content, API/UI route, bid score, fair value, collateral, reward, expiry, archive, event log, migration, RNG, or ledger mutation was introduced. | PASS | Working-tree diff and repository search; P3.4 files are the content export, pure visibility contract, and its test. |

## Commands

| Command | Exit code | Summary |
|---|---:|---|
| `pnpm --filter @nexus/content run test` | 0 | Facility, organization, and P3.4 contract tests passed; the contract test exercised the complete 2×2 eligibility matrix for both kinds (eight cases) and invalid kind rejection. |
| `pnpm --filter @nexus/content run typecheck` | 0 | Content package type check passed. |
| `pnpm --filter @nexus/simulation run test` | 0 | Existing PCG, energy, accrual, world-cycle, and fixed-point market suite passed. |
| `pnpm --filter @nexus/worker run test` | 0 | Forward-only deterministic worker world/market locking adapter passed. |
| `pnpm run plan:check` | 0 | Plan validation passed: 11 phases, 83 tasks, 28 completed tasks, 30 decisions. |
| `git diff --check` | 0 | No whitespace error in the tested working tree. |

## Defects and regressions

No P0 or P1 defect was observed.

## Explicitly not verified

- Fresh/returning persisted contract rows, migration rollback, recovery, concurrent offer competition, and authenticated API behavior are **NOT VERIFIED** because D-030 deliberately prohibits contract instances, persistence, bids, time windows, and endpoints in P3.4. They belong to P3.5/P3.6 and later tasks.
- Browser interaction, accessibility, player-facing contract board, campaign content, market result display, rewards, event log, and telemetry are **NOT VERIFIED**; none is delivered by P3.4.
- Root `pnpm verify`, E2E, benchmark/performance, backup/restore, release acceptance, P3.5-P3.8, and the Phase 3 regression gate are **NOT VERIFIED** by this narrow task-smoke run.

## Gate result

`PASS`
