# P2.2 Lifecycle Test Report

## Identity

| Field | Value |
|---|---|
| Mode | `task-smoke` |
| Phase/task | Phase 2 / `P2.2` — five facility types and level 1–12 catalog/schema |
| Build | `f6170ff`; P2.2 worktree changes were uncommitted at test time |
| Formula/content | `balance-1.2` / `asteria-baseline-0.2` |
| Environment | Windows 11 Pro `10.0.26200`; Node `v24.16.0`; pnpm `11.19.0`; Intel i7-7820HQ; 31.9 GiB RAM; Docker Compose PostgreSQL 17.7 / Redis 7.4.3 |
| Seed/fixtures | Catalog and schema path use no RNG; deterministic fixture profile `00000000-0000-0000-0000-000000000022`; disposable database `nexus_lifecycle_p22_smoke` |
| Test time | 2026-08-14T09:04:18Z, `lifecycle_game_tester` |

## Scope and observable journeys

| Journey/invariant | Expected observable behavior | Result | Evidence |
|---|---|---|---|
| Catalog boundaries | Exactly five facility kinds, each with levels 1–12 (60 rows); output, capital, and component values rise by level; upgrade time is at most six hours; storage is at least 24 hours. | PASS | `pnpm --filter @nexus/content run test` |
| Catalog invalid input | Lookup above level 12 is rejected. | PASS | Content test checks `facilityLevel("microgrid", 13)` throws. |
| Fresh profile | A newly created profile receives no automatic facility rows, per D-018. | PASS | Isolated PostgreSQL probe observed zero rows before explicit inserts. |
| Explicit facility persistence | The five allowed kinds, including boundary levels 1 and 12, persist and are readable after a separate database connection. | PASS | Probe inserted five rows; returning query returned exactly five rows with levels 1–12. |
| Invalid input and ownership boundary | Duplicate kind for a profile, unsupported kind, level 13, and orphan profile reference are rejected by database constraints. | PASS | Isolated PostgreSQL probe completed expected unique/check/foreign-key rejection paths. |
| Migration interruption/retry/recovery | Forward migration is repeatable; rollback refuses while a persisted facility exists, then succeeds after removal. Existing migration/ledger regression remains intact. | PASS | `pnpm --filter @nexus/api run test:integration` exit 0. |
| Service-window recovery | PostgreSQL and Redis were healthy before and after tests; disposable migration databases were absent after cleanup. | PASS | Final `pnpm services:check` and PostgreSQL cleanup query exit 0. |

## Commands and raw results

| Raw command | Exit | Result |
|---|---:|---|
| `python .agents/skills/verify-game-phase/scripts/validate_plan.py GAME_PLAN.md` | 0 | `PASS: 11 phases, 83 tasks, 18 completed tasks, 18 decisions validated` |
| `pnpm services:check` (initial preflight) | 1 | PostgreSQL service was not running; no application behavior was tested in this failed preflight. |
| `pnpm services:up; pnpm services:check; docker compose -f infra/docker-compose.yml ps` | 0 | Opened a stable local PostgreSQL/Redis window; both containers healthy. |
| `pnpm --filter @nexus/content run test` | 0 | `PASS: 60 versioned facility-level definitions satisfy the P2.2 bounds.` |
| `docker compose -f infra/docker-compose.yml exec -T postgres psql -U nexus_local -d postgres -v ON_ERROR_STOP=1 -c "DROP DATABASE IF EXISTS nexus_lifecycle_p22_smoke;" -c "CREATE DATABASE nexus_lifecycle_p22_smoke;"; $env:DATABASE_URL='postgresql://nexus_local:nexus_local_password@127.0.0.1:15432/nexus_lifecycle_p22_smoke'; pnpm --filter @nexus/api exec tsx src/migrations/cli.ts up` | 0 | Applied migrations `001` through `006_profile_facilities` to the isolated database. |
| `SQL probe piped to docker compose ... psql -d nexus_lifecycle_p22_smoke -v ON_ERROR_STOP=1` | 0 | Fresh profile had zero rows; five valid rows inserted; duplicate, invalid kind/level, and orphan-row operations rejected as expected. Query result: 5 rows, min level 1, max level 12. |
| `docker compose -f infra/docker-compose.yml exec -T postgres psql ... -d nexus_lifecycle_p22_smoke -c "SELECT facility_kind, level ..."; pnpm --filter @nexus/api run test:integration` | 0 | Separate-session read returned five kinds; integration reported migration/retry/rollback guard and ledger regressions PASS. |
| `docker compose ... -c "SELECT datname ... nexus_lifecycle_p22_smoke"; docker compose ... -c "DROP DATABASE nexus_lifecycle_p22_smoke;"; docker compose ... -c "SELECT count(*) ... LIKE 'nexus_lifecycle_p22%'"` | 0 | Exact disposable target verified, dropped, then count returned 0. |
| `pnpm verify; pnpm services:check; docker compose -f infra/docker-compose.yml exec -T postgres psql -U nexus_local -d postgres -v ON_ERROR_STOP=1 -c "SELECT count(*) AS temporary_test_dbs FROM pg_database WHERE datname IN ('nexus_migration_test', 'nexus_lifecycle_p22_smoke');"` | 0 | Full lint, typecheck, unit/content/API/web smoke, integration, deferred suites, and plan check passed; services healthy; temporary database count 0. |

The isolated SQL probe used only the five fictional facility identifiers and fixed UUID fixture stated above. It retained no database, screenshot, or trace artifact after cleanup.

## Findings

| ID | Severity | Reproduction | Expected / actual | Status |
|---|---|---|---|---|
| None | — | — | No P0/P1 (or other product) defect observed in P2.2 scope. | Closed |

## Explicitly skipped / not verified

- Browser/UI, keyboard, focus, color-independent state, and reduced-motion: **NOT VERIFIED / not in P2.2 scope**. No player-facing facility screen exists until P2.6.
- Build queue, upgrade completion, cancellation/refund, energy allocation, accrual, and 7/30-day balance simulation: **NOT VERIFIED / future P2.3–P2.7 scope**; no future feature was required for this smoke gate.
- Deterministic seed replay and Monte Carlo: **NOT VERIFIED / not applicable**. P2.2 catalog/schema has no RNG input; seed-dependent simulation is later scope.
- Performance, browser matrix, backup/restore, and release security suite: **NOT VERIFIED / release or later-phase scope**.

## Gate result

`PASS`
