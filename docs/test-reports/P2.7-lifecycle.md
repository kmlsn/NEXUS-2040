# P2.7 Lifecycle Test Report

## Result

`PASS`

## Test identity

| Field | Value |
|---|---|
| Mode | `task-smoke` |
| Phase/task | Phase 2 / `P2.7` — `balance-1.3` facility curves and deterministic 30-day full-ledger active/casual comparison |
| Commit/build | `1ec11637e399a242f40bfed03fe1cceb30756a54` with uncommitted P2.7 implementation work; Node `v24.16.0`, pnpm `11.19.0`, Python `3.14.5`, NumPy `2.3.5` |
| Formula/content version | `balance-1.3` / `asteria-baseline-0.2` for the P2 comparator; the plan retains `balance-1.2` for future P4 operation-resolution math |
| Environment | Windows host; Docker Compose PostgreSQL 17.7 and Redis 7.4.3, loopback services on `15432`/`16379` |
| Seed/fixture | RNG-independent deterministic fixture: fixed `START_MS=1800000000000`, 30 days, deterministic UUID sequence, 2/10 daily operation intents, and 24/48/72-hour claim cadences. The direct and full-regression executions emitted the same six values. |
| Date/agent | 2026-08-14 / `lifecycle_game_tester` |

## Player-facing journeys and invariants checked

| Journey/invariant | Expected observable behavior | Result | Evidence |
|---|---|---|---|
| Fresh profile: casual versus active 30-day economy | New isolated profiles with the same facilities and starting capital, differing only in 2 or 10 daily operation intents, must remain positive and keep the strict gap below 20%. | PASS | Full-ledger runner created six disposable profiles and returned: 24h `22961.417129` / `23507.116019` (+2.38%); 48h `7961.417129` / `8507.116019` (+6.85%); 72h `2961.417129` / `3507.116019` (+18.43%). Values match the plan’s rounded references. |
| Boundary claim cadence and foregone production | 24/48/72-hour claims must show decreasing facility value without a negative balance; 72h must be lower than daily. | PASS | Facility value was respectively `29999.999999`, `14999.999999`, and `9999.999999` normalized capital units for each activity cohort; all resource balances were non-negative. |
| Deterministic replay | Re-running the fixed, no-RNG comparator must reproduce all six ledger summaries. | PASS | Standalone P2.7 run and the later root integration run emitted identical JSON values. |
| Returning/persisted version profile and migration recovery | A persisted `balance-1.3` profile must prevent rollback; an empty migration must roll back, then the full migration chain must remain repeatable. | PASS | Migration integration exercised forward → rollback attempts, rejected rollback with a persisted `balance-1.3` profile, removed that disposable fixture, and completed legacy ledger/idempotency/queue rollback guards. |
| Invalid and integrity regression path | Existing invalid ledger intents, duplicate idempotency requests, immutable ledger entries, and concurrent debits must remain rejected or resolve once. | PASS | `pnpm test:integration` passed the five-resource atomic ledger, replay/conflict, immutable-ledger, and concurrent-spend checks before the P2.7 comparator. |
| Interrupted service/retry recovery | A test interruption must not be reported as a passed run; after services are restored, the complete regression command must pass. | PASS | Initial API integration run exited `1` after its migration segment when PostgreSQL disappeared (`57P03`, “database system is shutting down”). Recreated services were healthy; targeted migration + P2.7 retry and subsequent complete `pnpm test:integration` both exited `0`. No product assertion failed on the successful complete retry. |
| Cleanup and listener leak | Disposable P2.7/migration/ledger databases must be removed; no temporary HTTP server listeners may remain. | PASS | Final PostgreSQL catalog query found none of `nexus_migration_test`, `nexus_ledger_test`, or `nexus_p27_balance_test`; ports `3100`/`3101` had no listeners. Only the expected Compose service ports `15432`/`16379` remained. |
| Facility curves and independent balance reference | Facility monotonicity and capped timing plus the balance-1.3 cadence gate must pass the published model check. | PASS | `pnpm balance:check` passed the accrual fixture and Python balance audit, including monotonic facility curves, six-hour duration cap, and all three `<20%` cadence gaps. |

## Raw commands and results

| Command | Exit code | Raw outcome |
|---|---:|---|
| `py -3 -B .agents/skills/verify-game-phase/scripts/validate_plan.py GAME_PLAN.md` | 0 | `PASS: 11 phases, 83 tasks, 23 completed tasks, 26 decisions validated` |
| `pnpm services:up; pnpm services:check` | 0 | PostgreSQL and Redis containers became healthy. |
| `pnpm --filter @nexus/api run test:integration` | 1 | Migration segment printed PASS; the next segment received PostgreSQL `57P03` “database system is shutting down”. This interrupted run is retained as retry evidence, not treated as a successful check. |
| `pnpm services:check; pnpm services:logs` | 0 overall | Health check reported PostgreSQL not running; no retained container log was available after the service disappearance. |
| `pnpm services:up; pnpm services:check; pnpm --filter @nexus/api exec tsx --env-file-if-exists=../../.env scripts/migrations-integration.ts; pnpm --filter @nexus/api exec tsx --env-file-if-exists=../../.env scripts/p2-7-ledger-balance-integration.ts` | 0 | Healthy services; migration/version rollback path passed; deterministic P2.7 six-scenario full-ledger comparison passed. |
| `py -3 -c "import sys, numpy; print(sys.version.split()[0]); print(numpy.__version__)"; pnpm balance:check` | 0 | Python `3.14.5`, NumPy `2.3.5`; accrual fixture and balance-model audit passed. |
| `pnpm test:integration` | 0 | Services health, migration/version pinning, ledger/idempotency/concurrency regression, and P2.7 comparator all passed. |
| PostgreSQL temporary-DB catalog query plus `Get-NetTCPConnection` listener probe | 0 | Test databases absent; no listeners on `3100`/`3101`; expected service ports only. |

## Defects

No P0 or P1 defect was observed in the completed retry. The one failed run was an environment/service interruption: the Compose PostgreSQL container was absent immediately after the interruption and was recreated before the clean full-regression retry. It did not reproduce once services were restored.

## Explicitly not verified

- A real returning player profile completing an actual operation remains future Phase 4 scope; P2.7 intentionally uses the plan’s test-only normalized operation comparator (D-025).
- P2.8 property coverage for negative balances, clock regression, overflow, and broader concurrency boundaries is not implemented by this task; only its smallest critical existing ledger regression set was exercised.
- Browser matrix, accessibility traversal, performance benchmark, backup/restore, release acceptance, and live telemetry are outside this task-smoke scope and remain **NOT VERIFIED**.
